/**
 * Default organizer / escrow wallet address used across organizer and sponsor flows.
 * Single source of truth for Algorand escrow address (LogicSig flow).
 */
export const DEFAULT_ORGANIZER_ESCROW_ADDRESS =
  'VG57RMQO2W2FQUNWVSA7F55SNH5R2MKMSEZAGXT2NVDSTI4CCKPSOPZRDM'

/** Stateful escrow app ID for application calls (lock pool / release to winners). */
export const ESCROW_APP_ID = 755806920
