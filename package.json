{
  "name": "hackathon-prize-escrow",
  "version": "1.0.0",
  "description": "AI-Agent-Managed Hackathon Prize Escrow on Stellar",
  "type": "module",
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "dev:web": "npm run --prefix frontend dev",
    "create-escrow": "tsx scripts/create-escrow.ts",
    "deposit": "tsx scripts/deposit.ts",
    "release": "tsx scripts/release.ts",
    "agent": "tsx scripts/agent.ts",
    "deploy-contract": "tsx scripts/deploy-contract.ts",
    "build": "tsc --noEmit",
    "build:web": "npm run --prefix frontend build"
  },
  "dependencies": {
    "@stellar/freighter-api": "^6.0.1",
    "@stellar/stellar-sdk": "^15.1.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
